@model _221103018_OmerFarukBayraktutar.DAL.Models.Sehir
@{
    ViewData["Title"] = "Şehir Sil";
    Layout = "_AdminLayout";
}

<div class="container-fluid">
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Şehir Silme Onayı</h6>
        </div>
        <div class="card-body">
            <div class="alert alert-danger">
                <h5><i class="fas fa-exclamation-triangle"></i> Uyarı!</h5>
                <p>Bu şehri silmek istediğinizden emin misiniz? Bu işlem geri alınamaz.</p>
            </div>
            
            <dl class="row">
                <dt class="col-sm-3">Şehir ID:</dt>
                <dd class="col-sm-9">@Model.SehirId</dd>
                
                <dt class="col-sm-3">Şehir Adı:</dt>
                <dd class="col-sm-9">@Model.SehirAdi</dd>
            </dl>
            
            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="SehirId" />
                <div class="form-group">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash"></i> Sil
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> İptal
                    </a>
                </div>
            </form>
        </div>
    </div>
</div> 